fastapi
uvicorn[standard]
python-dotenv

# RAG stack — unpinned so pip resolves correctly for Python 3.13+
langchain
langchain-community
langchain-text-splitters
langchain-chroma
langchain-huggingface
chromadb
sentence-transformers

# Windows-only: pywin32
# pip install pywin32
# On macOS/Linux the active-window endpoint uses platform-native fallbacks
