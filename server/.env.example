# Copy this to .env and fill in values as needed
# The sidecar runs fully locally — no API keys required for Phase 1 RAG

# Optional: override the port (default 8765)
# SIDECAR_PORT=8765

# Optional: log level (DEBUG | INFO | WARNING)
# LOG_LEVEL=INFO
